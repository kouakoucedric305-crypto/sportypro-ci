# SportyPro CI v2.0 — requirements.txt
flask==3.1.0
requests==2.32.3
gunicorn==23.0.0
python-dotenv==1.0.1
# Pas de dépendances supplémentaires nécessaires —
# BallDontLie API est appelée directement via requests (gratuit, sans clé)
